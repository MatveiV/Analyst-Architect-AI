# Отчёт по выпускному проекту (упрощённая версия для сдачи) — REPORT.md

> Полный шаблон отчёта и сценарий защиты по `InitialTask.md` см. в
> [docs/graduation-report.md](docs/graduation-report.md) и [docs/defense-script.md](docs/defense-script.md).
> Здесь — блок, которого не хватало в отчёте: **мини-экономика отдельно по каждому модулю** и **6 сценариев пользователя (3+3)**.

---

## Сценарии пользователя (6)

### Вариант 2 — ИИ-рецензент ТЗ
1. **Рецензия «в один клик»:** аналитик вставляет текст ТЗ → через `POST /documents` + `POST /documents/{id}/review` получает структурированный отчёт (summary, риски, вопросы, критерии).
2. **Витрина рецензий + экспорт:** открывает `GET /reviews/{id}`, видит отчёт блоками, скачивает JSON/CSV (экспорт уже есть в UI и API).
3. **Честная неопределённость:** на сыром/противоречивом ТЗ система ставит метку «требует проверки» с причиной (`TOO_VAGUE_INPUT`/`CONTRADICTORY_INPUT`/`LOW_CONFIDENCE`) и 3+ вопросами заказчику.

### Вариант 5 — Система знаний
4. **Загрузка материалов:** сотрудник добавляет документы через `POST /kb/documents`, они появляются в витрине `GET /kb/documents`.
5. **Ответ с источниками:** задаёт вопрос через `POST /kb/ask` → получает ответ с цитатами `sources[]` и ссылками на документы по `document_id`.
6. **Данных недостаточно:** вопрос вне базы → честный ответ «данных недостаточно», `needs_review=true`, причина фиксируется в `qa_runs`/`audit_runs`.

---

## Мини-экономика «на салфетке» (отдельно по каждому модулю)

> Принято: ставка аналитика/оператора **2 500 ₽/час** (≈41,7 ₽/мин). LLM-затрата ~0,05–0,1 $/операция ≈ 5–10 ₽.

### Модуль 1 — ИИ-рецензент ТЗ (Вариант 2)
| Показатель | До (вручную) | После (система) |
|---|---|---|
| Время на одну операцию | 2–4 часа (≈10 000–20 000 ₽ по ставке) | 10–20 мин + проверка (≈500–1 000 ₽) + ~10 ₽ LLM |
| Стоимость 100 операций | 1 000 000–2 000 000 ₽ | 55 000–110 000 ₽ |
| **Вывод** | Система окупает себя уже на ~5–10 ТЗ и резко ускоряет подготовку документов к разработке; человек остаётся ответственным на этапе проверки рисков и вопросов. ||

### Модуль 2 — Система знаний (Вариант 5)
| Показатель | До (вручную) | После (система) |
|---|---|---|
| Время на поиск одного ответа (онбординг/FAQ) | 15–30 мин (≈600–1 250 ₽) | 1–3 мин + проверка источника (≈100–200 ₽) + ~10 ₽ LLM |
| Стоимость 100 ответов | 60 000–125 000 ₽ | 11 000–23 000 ₽ |
| **Вывод** | Главная ценность не только экономия денег, но **доказуемость** (каждый ответ с цитатами из `sources[]` и записью в `audit_runs`) и единая точка правды вместо «знаний в голове одного человека». ||

### Общий вывод
Оба модуля дают десятки-сотни тысяч ₽ экономии на каждые 100 операций и, главное, снижают риск «уверенного вранья»: система честно включает ручную проверку, что соответствует ценности выпускного проекта («не врёт уверенно»).

---

## Сверка с шаблоном отчёта `InitialTask.md`
Полные разделы (ценность, API, схема данных, ИИ-операция, память, КК, риски, план внедрения, портфолио, демо, сложности, план развития) заполнены в **`docs/graduation-report.md`**. Итоговая сверка по критериям приёмки Option2/Option5 — в **`AUDIT.md` §11**.